server:
  port: 8090

spring:
  application:
    name: account
  # 关闭 local 配置，避免冲突
  profiles:
    active:
  # 正确远程数据库配置（你的服务器地址）
  datasource:
    url: jdbc:mysql://8.153.163.48:3306/family_account?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: dev
    password: '@Web2026'
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

# 分页插件（你已经创建了配置类，这里不用重复）
# Sa-Token
sa-token:
  token-name: Authorization
  timeout: 86400
  is-concurrent: true
  is-share: false
  token-style: uuid